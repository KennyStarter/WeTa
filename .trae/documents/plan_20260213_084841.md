### 运行和调试计划

1. **安装依赖** - 运行 `npm install` 安装 dotenv 依赖
2. **配置环境变量** - 复制 `.env.example` 为 `.env`（需要配置火山方舟 API Key 和 Endpoint ID 才能使用完整功能）
3. **启动服务** - 运行 `npm start` 启动服务器
4. **访问应用** - 打开浏览器访问 http://localhost:3000

> 提示：完整功能需要在 `.env` 中配置 `ARK_API_KEY` 和 `ARK_ENDPOINT_ID`